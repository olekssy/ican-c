/* Universal helper functions for operations on arrays. */

#include "arrays.c"

void show_array(const int* arr, size_t arrSize);
int* duplicateArray(const int* arr, size_t arrSize);
void insertionSort(int* arr, size_t arrSize);
